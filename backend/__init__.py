"""Backend package for the bookstore Flask app.

Keep this file minimal: avoid importing application modules here to prevent
import-time side-effects and circular imports when running the package as
a module (for example `python -m backend.app`). Import `create_app` from
`backend.app` where you need it at runtime instead of exposing it here.
"""

__all__ = []
